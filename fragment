precision highp float;
uniform float time;
uniform vec2 resolution;
varying vec3 fPosition;
varying vec3 fNormal;

void main()
{
  vec3 col = abs(sin(2.0 * fNormal));
  
  gl_FragColor = vec4(col, 1.0);
}
